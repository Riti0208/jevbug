{
  "name": "jevbug",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "JevBug Experiment 0 — artificial life simulation using Jev as a decision provider, observing emergent communication with meaning-free signals.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit -p tsconfig.json && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "experiment": "tsx scripts/run-experiment.ts"
  },
  "dependencies": {
    "@ai-sdk/gateway": "^4.0.85",
    "ai": "^7.0.105",
    "three": "^0.170.0",
    "uplot": "^1.6.31"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/three": "^0.170.0",
    "@vercel/node": "^5.1.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
